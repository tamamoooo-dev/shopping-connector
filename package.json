{
  "name": "shopping-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless serverless connector (Cloudflare Worker): thin proxy + normalizer for shopping providers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dev:node": "node dev.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
